in iPython
S = 41
K = 40
r = .08
v = .30
T = 1
q = 0.0
from scipy.stats import norm
norm.cdf(0.0)
  output .5
from scipy.stats import norm as N
import numpy as np
d1 = ((np.log(S/K)) + (r - div - + .05*v*v) * T) / (v * np.sqrt(T))
  .4989....
 norm.cdf(d1)
  .69... expire in the money
d2 = d1 - v * np.sqrt(T)
c = s * np.exp(-q * T) * norm.cdf(d1) - K * np.exp(-r * T) * norm.cdf(d2)
c
  6.96...
